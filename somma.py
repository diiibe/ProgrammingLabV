numeri = [2, 5, 8, 3]
def somma(data):   # somma di una lista
    totale = 0
    for item in data:

numeri = [2,, 5, 8, 3]
def somma(numeri):   # somma di una lista
    totale = 0
    for item in numeri:
        total += item 
    return totale
print("Totale: {}".format(somma(numeri)))
